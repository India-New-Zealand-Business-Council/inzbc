# database — schema + migrations

Owner: Bhanu. `schema.sql` is the v0.1 data model (Postgres), grounded in the Intelligence
Database v1.9. Migrations land here once the internal-platform decision is made.
The Intelligence Database is the single Action Register.
