# SIP collector pipeline evidence

Generated by `scripts/sip_collector_pipeline_evidence.py` — a measured, offline run of `dedupe.py`, `ingest.py` and `source_register.py` against synthetic fixture data (all articles below are tagged `SYNTHETIC TEST DATA`; none is a real news item, real trade fact, or real SIP-185 write). Rerun the script to reproduce these counts; do not hand-edit this file.

**What this does not prove:** this does not exercise the live `SipPipelineClient` HTTP path, the database, or a real SIP-184 authorised run — see `run_dry_run.py` for that. It proves the pure dedupe/ingest/gate logic does what its docstrings claim, with counts from an actual run of that code, not from reading it.

## 1. Cross-run duplicate detection (`dedupe.py`)

| Incoming article | Matched against | Matched by |
| --- | --- | --- |
| 'SYNTHETIC TEST DATA - Kiwifruit exporters welcome new India quota' | — | not a duplicate |
| 'SYNTHETIC TEST DATA - FTA next steps (wire copy)' | `cand-001` | url |
| 'SYNTHETIC TEST DATA -   Dairy exporters watch India   tariff review' | `cand-002` | headline |
| 'SYNTHETIC TEST DATA - Manuka honey exporters see cut tariff take effect' | — | not a duplicate |
| 'SYNTHETIC TEST DATA - Wine exporters ask about MFN clause' | — | not a duplicate |

**2 of 5 well-formed incoming articles matched an already-captured candidate** — one by normalized url (case + trailing-slash difference), one by normalized headline (whitespace + wording difference, different url). Neither would have matched on exact string equality, which is what `find_duplicate_of` normalizes for.

## 2. Per-item isolation on a malformed article (`ingest.py`)

Ran the full 6-item batch above (including the 1 malformed item with no `title`) through `ingest_articles` in one call.

- Candidates created: **5**
- Items failed: **1**

Failures (isolated — did not stop the other items in the batch):

- `{'source_name': 'GDELT', 'headline': '', 'error': "'title'"}`

Created despite the malformed item sitting between them in the batch:

- 'SYNTHETIC TEST DATA - Kiwifruit exporters welcome new India quota'
- 'SYNTHETIC TEST DATA - FTA next steps (wire copy)'
- 'SYNTHETIC TEST DATA -   Dairy exporters watch India   tariff review'
- 'SYNTHETIC TEST DATA - Manuka honey exporters see cut tariff take effect'
- 'SYNTHETIC TEST DATA - Wine exporters ask about MFN clause'

## 3. Mandatory-source coverage gate reports rather than raises (`source_register.py`)

`MANDATORY_SOURCES` (SIP-185 v1.0 register): **112** sources.

With zero source outcomes recorded this run: `missing_mandatory_outcomes(set())` returns **112** ids — every mandatory source, as expected — and returns normally rather than raising. It is a report function, not a gate that blocks execution; the caller (SIP-184 step 4/11) is the one that must treat a non-empty result as a Critical stop before submitting a run for QA.

With outcomes recorded for the first 3 mandatory sources (`['NZ-OFF-001', 'NZ-OFF-002', 'NZ-OFF-003']`): **109** remain missing — confirms the function reports exactly the *uncovered* set, not a fixed total.

First 5 still-missing ids: `['NZ-OFF-004', 'NZ-OFF-005', 'NZ-OFF-006', 'NZ-OFF-007', 'NZ-OFF-008']`
