import './index.css'

// Replaced by AppShell in the next commit — kept minimal here so the package is compilable,
// lint-clean and testable from the very first commit.
export function App() {
  return (
    <main className="min-h-screen bg-slate-50 p-8">
      <h1 className="font-[family-name:var(--font-heading)] text-2xl font-bold uppercase text-inzbc-navy">
        INZBC SIP Review
      </h1>
    </main>
  )
}
