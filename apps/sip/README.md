# apps/sip — SIP control application

Core (auth, state machine, audit): Bhanu. Pipeline: Roshan (`apps/sip/pipeline`, `apps/sip/collector`).
UI: Paras (`apps/sip/ui`). Data model: `database/schema.sql`. Contract: `schemas/api-contract.md`.
State model: `schemas/state-machine.md`. See `docs/sip/`.
